class Solution {
public:
    bool rotateString(string s, string goal) {
        if(s.size() != goal.size()) return 0;
        string temp = s + s;
        int n = s.size();
        for(int i = 0; i < n; i++){
            if(temp.substr(i, n) == goal) return 1;
        }
        return 0;
    }
};
