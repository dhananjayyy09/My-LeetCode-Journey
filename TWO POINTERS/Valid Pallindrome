class Solution {
public:
    bool isPalindrome(string s) {
        string cleaned;
        for(char c : s){
            if(isalnum(c)){
                cleaned += tolower(c);
            }
        }
        string rever = cleaned;
        reverse(rever.begin(),rever.end());
        return rever == cleaned;
    }
};
