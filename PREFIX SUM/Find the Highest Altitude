class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int ans, preSum = 0;
        for(int it : gain){
            preSum += it;
            ans = max(ans, preSum);
        }
        return ans;
    }
};
