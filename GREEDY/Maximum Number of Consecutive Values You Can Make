class Solution {
public:
    int getMaximumConsecutive(vector<int>& coins) {
        int count = 0;
        sort(coins.begin(), coins.end());
        for(auto coin : coins){
            if(coin > count + 1) break;
            count += coin;
        }
        return count + 1;
    }
};
