class Solution {
public:
    long long countCommas(long long n) {
        long long base = 1000;
        long long ans = 0;
        while(n >= base){
            ans += (n - base + 1);
            base *= 1000;
        }
        return ans;
    }
};
